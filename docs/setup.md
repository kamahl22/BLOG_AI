# Setup Guide

1. Run `./setup.sh` to initialize the project.
2.میت

# Create root-level files
cat << 'EOF' > .gitignore
node_modules/
.env
__pycache__/
*.db
*.log
frontend/dist/
mobile/android/
mobile/ios/
